// ORDERED!

// Assessing - O(1)
// Searching  - O(N)

// Insertion:

// .push() - fast
// .pop() - fast

// shift() slow, because moving indexes... O(N)
// unshift() slow, because moving indexes... O(N)

let names = ['Michael', 'Melissa', 'Andrea'];

let values = [true, {}, 'Test'];

// push - O(1)
// pop - O(1)
// shift - O(N)
// unshift - O(N)
// concat - O(N)
// slice - O(N)
// splice - O(N)
// sort - O(N * log N)
// foreach/map/filter/reduce - O(N)
